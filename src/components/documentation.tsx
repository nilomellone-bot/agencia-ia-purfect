import { Badge, PageHeading, Panel } from './ui';
export function Documentation(){return <><PageHeading eyebrow="BASE TÉCNICA" title="Cómo funciona la Agencia" description="Una arquitectura coordinada y una base que puede crecer por etapas."/><Panel title="El recorrido de una decisión"><div className="architecture-flow">{['Datos','Métricas en código','Especialistas','Validación','Director','Recomendaciones','Aprobación humana','Implementación','Medición','Aprendizaje'].map((s,i)=><div key={s}><span>{String(i+1).padStart(2,'0')}</span><strong>{s}</strong><Badge tone={i<7?'blue':'neutral'}>{i<7?'Simulado en V0':'Etapa futura'}</Badge></div>)}</div></Panel><div className="two-column"><Panel title="Manager / Blackboard"><div className="padded-content"><p>El Director conserva el objetivo y consulta especialistas con una misma base de datos. Los especialistas no conversan libremente entre sí.</p><p>En la próxima etapa, el Manager utilizará especialistas como herramientas con OpenAI Agents SDK TypeScript y agent.asTool(). El expediente guarda las fuentes y las conclusiones de cada consulta.</p></div></Panel><Panel title="Reglas que el Director debe respetar"><div className="padded-content"><ul><li>Analytics bloquea decisiones con datos no confiables.</li><li>Finanzas bloquea recomendaciones que no sostienen la rentabilidad.</li><li>Meta Ads y CRO optimizan dentro de esos límites.</li><li>Las acciones sensibles requieren aprobación explícita.</li><li>Una aprobación nunca equivale a una implementación.</li></ul></div></Panel></div><Panel title="Qué funciona en esta versión"><div className="table-scroll"><table><thead><tr><th>Componente</th><th>V0</th><th>Siguiente etapa</th></tr></thead><tbody>{[['Dashboard y navegación','Next.js + React + Tailwind','Datos conectados'],['Métricas financieras','TypeScript, validación Zod','Fuentes conciliadas y políticas financieras completas'],['Director y especialistas','Flujo simulado, respuestas prediseñadas','Agents SDK en servidor'],['Expedientes y aprobaciones','Guardado en este navegador','Supabase con organización, usuarios y RLS'],['Implementación','Deshabilitada','Ejecutor con permisos y registro de auditoría'],['Memoria','Expedientes de ejemplo','Documentos y pgvector cuando aporte valor']].map(row=><tr key={row[0]}>{row.map((c,i)=><td key={i}>{c}</td>)}</tr>)}</tbody></table></div></Panel><Panel title="Estados de una recomendación"><div className="padded-content"><p>PROPOSED → VALIDATED → APPROVED → IMPLEMENTED → MEASURING → SUCCESSFUL / NEUTRAL / FAILED</p><p>CONFLICTED bloquea el avance. REJECTED cierra la propuesta. Un bloqueo solo puede resolverse con nueva evidencia y una nueva validación; no existe un botón para ignorarlo.</p><p>En esta V0 se pueden proponer, validar, bloquear, aprobar y rechazar ejemplos. Las implementaciones y los resultados reales quedan deshabilitados.</p></div></Panel></>;}
